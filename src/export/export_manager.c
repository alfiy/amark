// bool export_to_pdf(const char* md_path, const char* pdf_path) {
//     // 调用系统 Pandoc 命令行
//     char cmd[256];
//     snprintf(cmd, 256, "pandoc %s -o %s", md_path, pdf_path);
//     return system(cmd) == 0;
// }

// bool export_to_docx(const char* md_path, const char* docx_path) {
//     // 同理
// }
